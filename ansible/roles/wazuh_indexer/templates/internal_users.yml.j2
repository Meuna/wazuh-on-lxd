---
# This is the internal user database
# The hash value is a bcrypt hash and can be generated with plugin/tools/hash.sh

_meta:
  type: "internalusers"
  config_version: 2

# Define your internal users here

## Demo users

admin:
  hash: "{{ userpw | password_hash(hashtype='bcrypt') }}"
  reserved: true
  backend_roles:
  - "admin"
  description: "Demo admin user"

kibanaserver:
  hash: "{{ userpw | password_hash(hashtype='bcrypt') }}"
  reserved: true
  description: "Demo kibanaserver user"

kibanaro:
  hash: "{{ userpw | password_hash(hashtype='bcrypt') }}"
  reserved: false
  backend_roles:
  - "kibanauser"
  - "readall"
  description: "Demo kibanaro user"

logstash:
  hash: "{{ userpw | password_hash(hashtype='bcrypt') }}"
  reserved: false
  backend_roles:
  - "logstash"
  description: "Demo logstash user"

readall:
  hash: "{{ userpw | password_hash(hashtype='bcrypt') }}"
  reserved: false
  backend_roles:
  - "readall"
  description: "Demo readall user"

snapshotrestore:
  hash: "{{ userpw | password_hash(hashtype='bcrypt') }}"
  reserved: false
  backend_roles:
  - "snapshotrestore"
  description: "Demo snapshotrestore user"